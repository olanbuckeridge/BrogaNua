<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ie.dcu.computing.student.buckero2.broganua">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Main.ProductActivity" />
        <activity android:name=".Profile.UpdatePasswordActivity" />
        <activity android:name=".Profile.UpdateProfileActivity" />
        <activity
            android:name=".Authentication.ForgotPasswordActivity"
            android:theme="@style/AppTheme.noActionBar" />
        <activity
            android:name=".Authentication.RegistrationActvity"
            android:theme="@style/AppTheme.noActionBar" />
        <activity
            android:name=".Authentication.LoginActivity"
            android:theme="@style/AppTheme.noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main.MainActivity"
            android:theme="@style/AppTheme.noActionBar" />
        <activity android:name=".Profile.SettingsActivity" />
    </application>

</manifest>